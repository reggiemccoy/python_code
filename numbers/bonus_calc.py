base_salary = int(input('What is your base salary ? '))
# int has to add or else the number will combined and not added
base_bonus = int(input('What is the bonus ?'))
sum_total = (base_bonus + base_salary)
print(sum_total)
